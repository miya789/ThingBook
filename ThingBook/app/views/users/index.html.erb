<h1>Users#index</h1>
<p>Find me in app/views/users/index.html.erb</p>
<div class="container">
    <% @users.each do |user| %>
        <div class="user">
            <%= link_to("#{user.name}", "/users/#{user.id}") %>
        </div>
    <% end %>
</div>